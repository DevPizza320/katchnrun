export const game = {
    blockSize: Math.floor(window.innerHeight / 10), 
    blocksX: Math.ceil(window.innerWidth / Math.floor(window.innerHeight / 10)),
    blocksY: 10,
    cameraSpeed: 0.008 * .3
}